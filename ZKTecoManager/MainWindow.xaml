<Window x:Class="ZKTecoManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="ZKTeco Manager" Height="720" Width="1200"
        MinWidth="900" MinHeight="600"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Grid.Column="0" Background="{StaticResource PrimaryBrush}">
            <StackPanel>
                <!-- Logo area -->
                <Border Padding="16,24">
                    <StackPanel>
                        <TextBlock Text="ZKTeco" FontSize="22" FontWeight="Bold"
                                   Foreground="White"/>
                        <TextBlock Text="Manager" FontSize="14" Foreground="#B0C4DE"/>
                    </StackPanel>
                </Border>

                <Separator Background="#2E4D7A" Margin="0"/>

                <!-- Navigation items -->
                <StackPanel Margin="0,8">
                    <Button Content="Dispositivos"    Style="{StaticResource NavButton}" Tag="Devices"/>
                    <Button Content="Empleados"       Style="{StaticResource NavButton}" Tag="Employees"/>
                    <Button Content="Asistencia"      Style="{StaticResource NavButton}" Tag="Attendance"/>
                    <Button Content="Incidencias"     Style="{StaticResource NavButton}" Tag="Incidents"/>
                    <Button Content="Reportes"        Style="{StaticResource NavButton}" Tag="Reports"/>
                    <Button Content="Configuracion"   Style="{StaticResource NavButton}" Tag="Settings"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Content area -->
        <Border Grid.Column="1" Padding="24">
            <ContentControl x:Name="MainContent">
                <TextBlock Text="Selecciona un modulo del menu lateral."
                           FontSize="16" Foreground="{StaticResource TextSecondaryBrush}"
                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </ContentControl>
        </Border>
    </Grid>
</Window>
