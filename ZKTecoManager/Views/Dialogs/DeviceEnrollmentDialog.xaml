<Window x:Class="ZKTecoManager.Views.Dialogs.DeviceEnrollmentDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Enrolamiento en Dispositivos" Width="580" Height="440"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BackgroundBrush}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Enrolamiento en Dispositivos"
                   Style="{StaticResource SectionHeader}" Margin="0,0,0,4"/>
        <TextBlock Grid.Row="1" x:Name="TxtEmployeeName"
                   Foreground="{StaticResource TextSecondaryBrush}"
                   FontSize="13" Margin="0,0,0,16"/>

        <!-- Device list -->
        <Border Grid.Row="2" Style="{StaticResource Card}" Padding="0">
            <DataGrid x:Name="DeviceGrid"
                      Style="{StaticResource ModernDataGrid}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False" IsReadOnly="True"
                      RowHeight="44">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Dispositivo" Binding="{Binding DeviceName}" Width="150"/>
                    <DataGridTextColumn Header="IP"          Binding="{Binding IpAddress}"  Width="110"/>
                    <DataGridTextColumn Header="Ubicación"   Binding="{Binding Location}"   Width="110"/>
                    <DataGridTemplateColumn Header="Estado" Width="90">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border CornerRadius="10" Padding="8,3" HorizontalAlignment="Left" Margin="6,0"
                                        Background="{Binding StatusBackground}">
                                    <TextBlock Text="{Binding StatusLabel}" FontSize="11" Foreground="White"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="Acción" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Width="80" Height="28" Margin="6,0" FontSize="11"
                                        Content="{Binding ActionLabel}"
                                        Style="{Binding ActionStyle}"
                                        IsEnabled="{Binding IsIdle}"
                                        Click="ActionButton_Click"
                                        Tag="{Binding}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- Status -->
        <TextBlock Grid.Row="3" x:Name="TxtStatus"
                   Foreground="{StaticResource TextSecondaryBrush}"
                   FontSize="12" Margin="0,10,0,0" TextWrapping="Wrap"/>

        <!-- Close -->
        <Button Grid.Row="4" Content="Cerrar" Width="90" Height="34"
                HorizontalAlignment="Right" Margin="0,14,0,0"
                Style="{StaticResource PrimaryButton}" Click="Close_Click"/>
    </Grid>
</Window>
