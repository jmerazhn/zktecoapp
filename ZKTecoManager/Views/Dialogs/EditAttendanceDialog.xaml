<Window x:Class="ZKTecoManager.Views.Dialogs.EditAttendanceDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Editar Registro" Width="400" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{StaticResource BackgroundBrush}">

    <StackPanel Margin="24">

        <TextBlock Text="Editar Registro de Asistencia"
                   Style="{StaticResource SectionHeader}" Margin="0,0,0,4"/>

        <!-- Employee info -->
        <Border Background="#F0F4FF" CornerRadius="6" Padding="12,10" Margin="0,0,0,18">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Grid.Column="0" Text="Empleado: "
                           FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
                <TextBlock Grid.Row="0" Grid.Column="1" x:Name="TxtEmployee"
                           FontSize="12" FontWeight="SemiBold"/>
                <TextBlock Grid.Row="1" Grid.Column="0" Text="Fecha: "
                           FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
                <TextBlock Grid.Row="1" Grid.Column="1" x:Name="TxtDate"
                           FontSize="12" FontWeight="SemiBold"/>
            </Grid>
        </Border>

        <!-- Status -->
        <TextBlock Text="Estado *" Style="{StaticResource FieldLabel}"/>
        <ComboBox x:Name="CmbStatus" Padding="8,6" FontSize="13" Margin="0,0,0,14"/>

        <!-- Notes -->
        <TextBlock Text="Notas / Justificación" Style="{StaticResource FieldLabel}"/>
        <TextBox x:Name="TxtNotes" Style="{StaticResource ModernTextBox}"
                 Height="80" TextWrapping="Wrap" AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto" Margin="0,0,0,24"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancelar" Width="90" Height="34" Margin="0,0,8,0" Click="Cancel_Click">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="#E5E7EB" CornerRadius="4" Padding="12,0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button Content="Guardar" Width="90" Height="34"
                    Style="{StaticResource PrimaryButton}" Click="Save_Click"/>
        </StackPanel>

    </StackPanel>
</Window>
